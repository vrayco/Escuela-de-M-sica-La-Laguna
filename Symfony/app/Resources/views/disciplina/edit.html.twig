{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Disciplinas{% endblock %}

{% form_theme edit_form 'form/fields.html.twig' %}

{% block content %}
    {{ form_start(edit_form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="row">
        <div class="input-field col s12">
            {{ form_widget(edit_form.nombre) }}
            {{ form_label(edit_form.nombre) }}
        </div>
    </div>
    <div class="row">
        <div class="input-field col s6">
            {{ form_widget(edit_form.edadMinima) }}
            {{ form_label(edit_form.edadMinima) }}
        </div>
        <div class="input-field col s6">
            {{ form_widget(edit_form.edadMaxima) }}
            {{ form_label(edit_form.edadMaxima) }}
        </div>
    </div>
    <div class="row">
        <div class="input-field col s12">
            {{ form_widget(edit_form.disciplinaGrupo) }}
            {#{{ form_label(edit_form.disciplinaGrupo) }}#}
        </div>
    </div>

    {{ form_rest(edit_form) }}
    <div class="col s12 center-align">
        <div class="fixed-action-btn horizontal" style="bottom: 45px; right: 24px;">
            <button class="btn-floating btn-large waves-effect waves-light" type="submit" name="action">
                <i class="large material-icons">done</i>
            </button>
        </div>
    </div>
    {{ form_end(edit_form) }}

{% endblock %}

{% block javascripts %}

    <script>
        $( document ).ready(function() {

            $('select').material_select();

        });
    </script>

{% endblock %}