{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Especialidades{% endblock %}

{% form_theme edit_form 'form/fields.html.twig' %}
{% block content %}

    {{ form_start(edit_form) }}
    <div class="row">
        <div class="input-field col s12">
            {{ form_widget(edit_form.disciplina) }}
            {#{{ form_label(form.disciplina) }}#}
        </div>
    </div>
    <div class="row">
        <div class="col s4">
            {{ form_widget(edit_form.entraEnSorteo) }}
            {{ form_label(edit_form.entraEnSorteo) }}
        </div>
    </div>
    <div class="row">
        <div class="input-field col s6">
            {{ form_widget(edit_form.numeroPlazas) }}
            {{ form_label(edit_form.numeroPlazas) }}
        </div>
        <div class="input-field col s6">
            {{ form_widget(edit_form.numeroPlazasPrioritarias) }}
            {{ form_label(edit_form.numeroPlazasPrioritarias)  }}
        </div>
    </div>
    {{ form_rest(edit_form) }}
    <div class="col s12 center-align">
        <div class="fixed-action-btn horizontal" style="bottom: 45px; right: 24px;">
            <button class="btn-floating btn-large waves-effect waves-light" type="submit" name="action">
                <i class="large material-icons">done</i>
            </button>
        </div>
    </div>
    {{ form_end(edit_form) }}
{% endblock %}

{% block javascripts %}
    <script>
        $( document ).ready(function() {

            $('select').material_select();

            $( "input" ).change(function() {
                $(this).removeClass('invalid');
            });

            {% if form_errors(edit_form.disciplina)|length > 0 %}
            Materialize.toast("{{ form_errors(edit_form.disciplina) }}",4000, "error", function(){$('#toast-container').remove()});
            {% endif %}

        });
    </script>
{% endblock %}